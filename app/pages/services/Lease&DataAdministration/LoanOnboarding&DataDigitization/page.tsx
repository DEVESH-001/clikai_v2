
import { LoanOnboardingDigitizationMetaData } from "@/app/metadata";

import LoanOnboardingPage from "./LoanOnboardingDigitizationContent";

export const metadata = LoanOnboardingDigitizationMetaData;

export default function ContactUsPage() {
  return <LoanOnboardingPage />;
}
